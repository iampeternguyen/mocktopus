{
    "name": "mock",
    "version": "1.0.0",
    "main": "app.js",
    "scripts": {
        "start": "npm run dev",
        "dev": "nodemon src/app.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "description": "A proxy server that routes requests to a primary server with a fallback.",
    "dependencies": {
        "express": "^4.21.0",
        "http-proxy-middleware": "^2.0.1",
        "openai": "^4.66.1",
        "swagger-parser": "^10.0.3"
    },
    "devDependencies": {
        "nodemon": "^3.1.7"
    }
}
